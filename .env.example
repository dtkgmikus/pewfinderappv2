VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Where the web app is deployed — used only to send the iOS/Android app to
# the web billing console (see MOBILE.md: Stripe checkout is intentionally
# not started from inside the native apps).
VITE_SITE_URL=https://getgod.com
